# Shell scripts must keep LF line endings or they break when sourced on macOS/Linux
*.sh   text eol=lf
# Keep the PowerShell profile LF too, so the raw download is byte-consistent everywhere
*.ps1  text eol=lf
